import { ImageIcon } from "lucide-react";

const PostImage = () => {
  return (
    <div className="bg-card flex aspect-square w-full items-center justify-center rounded">
      <ImageIcon />
    </div>
  );
};

export default PostImage;
